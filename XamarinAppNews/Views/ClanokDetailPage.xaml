<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="zadanie1_kanas.Views.ClanokDetailPage"
             Title="">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <WebView x:Name="pageWebView" VerticalOptions="FillAndExpand" HorizontalOptions="Fill" Grid.Row="0" Grid.Column="0"/>
    </Grid>
</ContentPage>